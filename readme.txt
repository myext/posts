1. Work directory is /public.
2. Enter your settings ( DB ) in Src/config/config.php
3. Run php Src/Db/migrate.php to create DB and dummy data.
   Data shared between two tables ( users and posts ). It will helpful if add authorization.
